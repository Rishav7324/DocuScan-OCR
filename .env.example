# No external API keys are required.
# DocuScan OCR runs fully on-device: OCR via ML Kit, encryption via Android Keystore.
# Cloud sync (Cloudflare R2 / Google Drive / Dropbox) uses credentials you enter in the app's Cloud Sync panel.
